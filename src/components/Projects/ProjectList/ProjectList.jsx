// Dependencies
import React          from 'react'
import { withNavbar } from '../../withNavbar'

const ProjectList = () => {
  return (
    <div>
      ProjectList
    </div>
  )
}

export default withNavbar(ProjectList)